import localFont from "next/font/local";

const PixelDigivolve = localFont({
  src: [
    {
      path: "../../public/fonts/Pixel-Digivolve.otf",
    },
  ],
  variable: "--font-pixel-raw",
});

export default PixelDigivolve;
